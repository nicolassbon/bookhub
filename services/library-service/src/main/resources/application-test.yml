spring:
  config:
    activate:
      on-profile: test
  security:
    oauth2:
      resourceserver:

jwt:
  issuer: test-issuer
  audience: test-audience



catalog:
  service:
    base-url: http://localhost:19999

server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        include: health
