spring:
  application:
    name: catalog-service
  threads:
    virtual:
      enabled: true
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:default}
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/bookhub_catalog}
    username: ${DB_USERNAME:bookhub}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: ${FLYWAY_ENABLED:true}
    locations: ${FLYWAY_LOCATIONS:classpath:db/migration}

server:
  port: ${SERVER_PORT:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

catalog:
  providers:
    openlibrary:
      enabled: ${CATALOG_OPENLIBRARY_ENABLED:true}
      url: ${CATALOG_OPENLIBRARY_URL:https://openlibrary.org}
      timeout-ms: ${CATALOG_OPENLIBRARY_TIMEOUT_MS:2000}
