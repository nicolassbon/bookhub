# Prometheus configuration

This folder contains the initial Prometheus scaffold for local observability.

Current state:
- basic scrape configuration
- placeholder service targets aligned with the BookHub service names
